CREATE TABLE riders (id SERIAL PRIMARY KEY, name VARCHAR(100) NOT NULL, vehicle VARCHAR(50) NOT NULL, total_deliveries INT NOT NULL DEFAULT 0); CREATE TABLE reviews (id SERIAL PRIMARY KEY, rider_id INT NOT NULL REFERENCES riders(id), customer_name VARCHAR(100) NOT NULL, rating INT NOT NULL CHECK (rating BETWEEN 1 AND 5), comment VARCHAR(500) DEFAULT NULL);